# # For production (Vercel uses /api automatically)

# Key: VITE_API_BASE
# Value: /api
# Environment: Production

# For local dev, override if backend runs on 127.0.0.1
 VITE_API_BASE=http://127.0.0.1:8000
